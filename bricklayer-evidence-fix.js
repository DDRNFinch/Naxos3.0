function back(){if(state.screen==='activity'){state.screen=state.duty?'duty':'course'}else if(state.screen==='duty'){state.screen='course'}else{state.screen='home'}render();window.scrollTo(0,0)}
function dutyView(){const d=state.duty;return `<main class="page">${header(d.title,`DUTY ${d.id.slice(1)}`)}<section class="duty-statement"><small>ACTUAL DUTY</small><p>${esc(d.summary)}</p></section><div class="master-map">${ksbGroup('Skills',d.S)}${ksbGroup('Knowledge',d.K)}${ksbGroup('Behaviours',d.B)}</div><div class="course-rule"><b>Evidence route</b><span>The duty mapping remains authoritative. Practical tasks below are evidence routes only. A K/S/B already evidenced elsewhere is not automatically repeated.</span></div><h2 class="section-title">Practical evidence tasks</h2><div class="pack-list">${NAXOS_BRICKLAYER_EVIDENCE_MAP.activities.map(activityCard).join('')}</div></main>`}
Object.assign(window,{back});
